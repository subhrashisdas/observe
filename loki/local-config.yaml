auth_enabled: false

server:
  http_listen_port: 3100

ingester:
  chunk_idle_period: 5m
  chunk_retention_period: 30m
  max_transfer_retries: 0

schema:
  configs:
    - from: 2020-10-24
      store: boltdb
      schema: v11
      index:
        prefix: index_
        period: 24h

storage:
  type: filesystem
  filesystem:
    directory: /loki/index

compactor:
  retention_enabled: true
